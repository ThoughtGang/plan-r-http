%h1
  = t.app.search.index_keywords_title
.search-index-info
  - @results.each do |name, results|
    %h2.plugin-name #{name}
    %pre.search-index-keywords
      -# FIXME: format results
      #{results.inspect}
