%h1
  = t.app.search.index_docs_title
.search-index-info
  - @results.each do |name, results|
    %h2.plugin-name #{name}
    %pre.search-index-documents
      -# FIXME: format results
      #{results.inspect}
