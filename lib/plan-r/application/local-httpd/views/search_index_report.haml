%h1
  = t.app.search.index_report_title
.search-index-info
  - @results.each do |name, results|
    %h2.plugin-name #{name}
    %pre.search-index-report
      -# FIXME: format results
      #{results.inspect}
